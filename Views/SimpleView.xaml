<Window
    x:Class="ChangeEditOpacity.Views.SimpleView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ChangeEditOpacity.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

    DataContext="{Binding RelativeSource={RelativeSource Self}}"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d"
    Width="350"  
    Height="200"
    Title="Set Inactive Component Opacity"
    Background="#FF3B4453"
    
    WindowStyle="None"            
    AllowsTransparency="True"     
    ResizeMode="NoResize"
    xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero2">
    <Window.Resources>

        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="TextBox.Static.Background" Color="#FFFFFFFF"/>

        
        <Style x:Key="RepeatButtonTransparent" TargetType="{x:Type RepeatButton}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Rectangle Fill="{TemplateBinding Background}" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <SolidColorBrush x:Key="SliderThumb.Static.Background" Color="#FF454F61"/>
        <SolidColorBrush x:Key="SliderThumb.Static.Border" Color="#FF2C3440"/>
  
        <SolidColorBrush x:Key="SliderThumb.Static.Foreground" Color="#FFE1E1E1"/>
        
        <SolidColorBrush x:Key="SliderThumb.MouseOver.Background" Color="#FF5E697C"/>
        
        <SolidColorBrush x:Key="SliderThumb.MouseOver.Border" Color="#FF009FE3"/>
        
        <SolidColorBrush x:Key="SliderThumb.Pressed.Background" Color="#FF009FE3"/>
        
        <SolidColorBrush x:Key="SliderThumb.Pressed.Border" Color="#FF1E262E"/>
        
        <SolidColorBrush x:Key="SliderThumb.Disabled.Background" Color="#FF6F7A8D"/>
        
        <SolidColorBrush x:Key="SliderThumb.Disabled.Border" Color="#FF535A69"/>
        <SolidColorBrush x:Key="SliderThumb.Track.Background" Color="#FF2C3440"/>
        
        <SolidColorBrush x:Key="SliderThumb.Track.Border" Color="#FF1E262E"/>
        

        
        <ControlTemplate x:Key="SliderThumbHorizontalDefault" TargetType="{x:Type Thumb}">
            
            <Grid HorizontalAlignment="Center" UseLayoutRounding="True" VerticalAlignment="Center">
                <Rectangle x:Name="grip" 
                           Width="11" 
                           Height="18" 
                           Fill="{StaticResource SliderThumb.Static.Background}" 
                           Stroke="{StaticResource SliderThumb.Static.Border}" 
                           StrokeThickness="1"
                           RadiusX="1" RadiusY="1"  
                           SnapsToDevicePixels="True" 
                           UseLayoutRounding="True" 
                           VerticalAlignment="Center"/>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="true">
                    <Setter Property="Fill" TargetName="grip" Value="{StaticResource SliderThumb.MouseOver.Background}"/>
                    <Setter Property="Stroke" TargetName="grip" Value="{StaticResource SliderThumb.MouseOver.Border}"/>
                </Trigger>
                <Trigger Property="IsDragging" Value="true">
                    <Setter Property="Fill" TargetName="grip" Value="{StaticResource SliderThumb.Pressed.Background}"/>
                    <Setter Property="Stroke" TargetName="grip" Value="{StaticResource SliderThumb.Pressed.Border}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="false">
                    <Setter Property="Fill" TargetName="grip" Value="{StaticResource SliderThumb.Disabled.Background}"/>
                    <Setter Property="Stroke" TargetName="grip" Value="{StaticResource SliderThumb.Disabled.Border}"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- Slider Track Template (Horizontal) - Only necessary changes applied -->
        <ControlTemplate x:Key="SliderHorizontal" TargetType="{x:Type Slider}">
            <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TickBar x:Name="TopTick" Fill="{TemplateBinding Foreground}" Height="4" Margin="0,0,0,2" Placement="Top" Grid.Row="0" Visibility="Collapsed"/>
                    <TickBar x:Name="BottomTick" Fill="{TemplateBinding Foreground}" Height="4" Margin="0,2,0,0" Placement="Bottom" Grid.Row="2" Visibility="Collapsed"/>

                    <!-- Track Background - Used the custom track background brush -->
                    <Border x:Name="TrackBackground" 
                            Background="{StaticResource SliderThumb.Track.Background}" 
                            BorderBrush="{StaticResource SliderThumb.Track.Border}" 
                            BorderThickness="1" 
                            Height="6.0" 
                        CornerRadius="3"
                            Margin="5,0" 
                            Grid.Row="1" 
                            VerticalAlignment="center">
                        <Canvas Margin="-6,-1">
                            <!-- Selection Range (the filled part of the slider) - Use Inventor Blue -->
                            <Rectangle x:Name="PART_SelectionRange" 
                                       Fill="#FF009FE3" 
                                       Height="6.0" 
                                       Visibility="Hidden"/>
                        </Canvas>
                    </Border>

                    <Track x:Name="PART_Track" Grid.Row="1">
                        <Track.DecreaseRepeatButton>
                            <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Style="{StaticResource RepeatButtonTransparent}"/>
                        </Track.DecreaseRepeatButton>
                        <Track.IncreaseRepeatButton>
                            <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Style="{StaticResource RepeatButtonTransparent}"/>
                        </Track.IncreaseRepeatButton>
                        <Track.Thumb>
                            <Thumb x:Name="Thumb" Focusable="False" Height="18" OverridesDefaultStyle="True" Template="{StaticResource SliderThumbHorizontalDefault}" VerticalAlignment="Center" Width="11"/>
                        </Track.Thumb>
                    </Track>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="TickPlacement" Value="TopLeft">
                    <Setter Property="Visibility" TargetName="TopTick" Value="Visible"/>
                    <Setter Property="Margin" TargetName="TrackBackground" Value="5,2,5,0"/>
                </Trigger>
                <Trigger Property="TickPlacement" Value="BottomRight">
                    <Setter Property="Visibility" TargetName="BottomTick" Value="Visible"/>
                    <Setter Property="Margin" TargetName="TrackBackground" Value="5,0,5,2"/>
                </Trigger>
                <Trigger Property="TickPlacement" Value="Both">
                    <Setter Property="Visibility" TargetName="TopTick" Value="Visible"/>
                    <Setter Property="Visibility" TargetName="BottomTick" Value="Visible"/>
                </Trigger>
                <Trigger Property="IsSelectionRangeEnabled" Value="true">
                    <Setter Property="Visibility" TargetName="PART_SelectionRange" Value="Visible"/>
                </Trigger>
                <Trigger Property="IsKeyboardFocused" Value="true">
                    <Setter Property="Foreground" TargetName="Thumb" Value="#FF009FE3"/>
                    
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- Slider Style - using the Horizontal template -->
        <Style x:Key="OpacitySliderStyle" TargetType="{x:Type Slider}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource SliderThumb.Static.Foreground}"/>
            <Setter Property="Template" Value="{StaticResource SliderHorizontal}"/>
            
        </Style>

        <!-- Button Style - Updated to use the Button style defined below, using an explicit Key -->
        <Style x:Key="InventorButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF454F61"/>
            <!-- Default Button Color (Inventor-like gray) -->
            <Setter Property="Foreground" Value="#FFE1E1E1"/>
            
            <Setter Property="BorderBrush" Value="#FF2C3440"/>
            
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FF5E697C"/>
                                
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#FF009FE3"/>
                                
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FF009FE3"/>
                                

                                
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- The default SliderVertical and other unused styles were removed for brevity, 
             but they can be added back if needed for complete theme fidelity. -->

    </Window.Resources>

    <Border Background="#FF3B4453" 
            BorderBrush="#FF1E262E" BorderThickness="1" CornerRadius="2">
        <StackPanel>
            
            <Grid Height="25" Background="#FF2C3440">
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                           Foreground="#FFE1E1E1" 
                           FontWeight="Bold"
                           VerticalAlignment="Center" HorizontalAlignment="Left" Margin="8,0,0,0"/>
            </Grid>

            <StackPanel Margin="20">

                <TextBlock Text="Inactive Components Shade Opacity (%)" 
                           FontWeight="Bold" 
                           Foreground="#FFE1E1E1" 
                           FontSize="14" 
                           Margin="0,15,0,10" 
                           HorizontalAlignment="Center"/>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="0%" 
                               Foreground="#FFE1E1E1" 
                               VerticalAlignment="Center" 
                               Grid.Column="0" 
                               Margin="0,0,10,0"/>

                    
                    <Slider Style="{DynamicResource OpacitySliderStyle}" 
                            x:Name="OpacitySlider" 
                            Minimum="0" 
                            Maximum="100"
                            Grid.Column="1"
                            Value="{Binding InitialOpacityValue, Mode=TwoWay}" 
                            Margin="0,0,0,0" />

                    
                    <TextBlock Text="{Binding ElementName=OpacitySlider, Path=Value, StringFormat={}{0:F0}%}" 
                               Foreground="#FFE1E1E1" 
                               VerticalAlignment="Center" 
                               Grid.Column="2" 
                               Margin="10,0,0,0" 
                               Width="40"/>
                </Grid>

                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,0">

                    
                    <Button x:Name="OkButton" Content="OK" Style="{DynamicResource InventorButtonStyle}" Width="80" Margin="5,0"/>
                    <Button x:Name="CancelButton" Content="Cancel" Style="{DynamicResource InventorButtonStyle}" Width="80" Margin="5,0"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
